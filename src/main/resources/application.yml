spring:
  application:
    name: account
  datasource:
    password: root
    url: jdbc:mysql://localhost:3306/account
    username: root
    driver-class-name: com.mysql.cj.jdbc.Driver  
  jpa:
    hibernate:
      ddl-auto: update
server:
  port: 9092
  
